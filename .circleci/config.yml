version: 2
jobs:
  build:
    working_directory: ~/giantswarm-js-client
    docker:
      - image: circleci/node:6-stretch
    steps:
      - checkout

      - run:
          name: Update npm
          command: |
            npm --version
            npm set progress=false && sudo npm install -g process-nextick-args util-deprecate && sudo npm install -g npm@latest
            npm --version

      - restore_cache:
          key: dependency-cache-{{ checksum "package.json" }}

      - run:
          name: Install dependencies from package.json
          command: npm install

      - save_cache:
          key: dependency-cache-{{ checksum "package.json" }}
          paths:
            - ./node_modules

      - run:
          name: Check for dependency updates
          command: |
            npm install -g npm-check-updates
            ncu

      - run:
          name: Run tests
          command: npm test
