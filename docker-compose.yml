version: "2"
services:

  jsclient:
    build: ./
    image: giantswarm-js-client-tests
    command: npm test

  mockapi:
    image: quay.io/giantswarm/mock-api:latest
    ports:
      - "8000:8000"
    environment:
      DELAY_MS: 0
